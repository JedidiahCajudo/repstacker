<%= form_with model: @workout, local: true do |form| %>
  <% if form.object.errors.any? %>
    <p class="error"><%= form.object.errors.full_messages.first %></p>
  <% end %>

<div>
  <%= form.label :date, "Date" %>
  <%= form.date_field :date %>
</div>

<%= form.fields_for :exercises do |exercise_fields| %>
  <div class="exercise-fields">
    <%= exercise_fields.label :name, "Lift" %>
    <%= exercise_fields.text_field :name %>

    <%= exercise_fields.label :weight, "Weight(kg)" %>
    <%= exercise_fields.text_field :weight, value: 0 %>

    <%= exercise_fields.label :repetitions, "Reps" %>
    <%= exercise_fields.number_field :repetitions, value: 0 %>


  </div>
<% end %>


<div>
  <%= form.submit "Save Workout" %>
</div>

<% end %>
